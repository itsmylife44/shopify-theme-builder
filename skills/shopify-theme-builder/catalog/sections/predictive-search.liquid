{% comment %}Search suggestions the header search box shows as the customer types: queries, products, collections and pages from /search/suggest.{% endcomment %}
{% if predictive_search.performed %}
  {% assign terms = predictive_search.terms | escape %}
  <div class="predictive-search">
    {% if predictive_search.resources.queries.size > 0 %}
      <div role="group" aria-labelledby="PredictiveSearchQueries">
        <p id="PredictiveSearchQueries" class="predictive-search__heading text-label" role="presentation">
          {{ 'search.suggestions' | t }}
        </p>
        {% for query in predictive_search.resources.queries %}
          <a
            id="PredictiveSearchQuery-{{ forloop.index }}"
            class="predictive-search__option"
            href="{{ query.url }}"
            role="option"
            aria-selected="false"
            tabindex="-1"
          >
            {{- query.styled_text -}}
          </a>
        {% endfor %}
      </div>
    {% endif %}

    {% if predictive_search.resources.products.size > 0 %}
      <div role="group" aria-labelledby="PredictiveSearchProducts">
        <p id="PredictiveSearchProducts" class="predictive-search__heading text-label" role="presentation">
          {{ 'search.products' | t }}
        </p>
        {% for product in predictive_search.resources.products %}
          <a
            id="PredictiveSearchProduct-{{ forloop.index }}"
            class="predictive-search__option predictive-search__product"
            href="{{ product.url }}"
            role="option"
            aria-selected="false"
            tabindex="-1"
          >
            {% if product.featured_image %}
              {{
                product.featured_image
                | image_url: width: 120
                | image_tag: widths: '60, 120', sizes: '3rem', alt: '', class: 'predictive-search__image'
              }}
            {% else %}
              {{ 'product-1' | placeholder_svg_tag: 'placeholder predictive-search__image' }}
            {% endif %}
            <span>
              {{ product.title | escape }}
              <span class="predictive-search__price text-small">{{ product.price | money }}</span>
            </span>
          </a>
        {% endfor %}
      </div>
    {% endif %}

    {% if predictive_search.resources.collections.size > 0 %}
      <div role="group" aria-labelledby="PredictiveSearchCollections">
        <p id="PredictiveSearchCollections" class="predictive-search__heading text-label" role="presentation">
          {{ 'collections.title' | t }}
        </p>
        {% for collection in predictive_search.resources.collections %}
          <a
            id="PredictiveSearchCollection-{{ forloop.index }}"
            class="predictive-search__option"
            href="{{ collection.url }}"
            role="option"
            aria-selected="false"
            tabindex="-1"
          >
            {{- collection.title | escape -}}
          </a>
        {% endfor %}
      </div>
    {% endif %}

    {% if predictive_search.resources.pages.size > 0 %}
      <div role="group" aria-labelledby="PredictiveSearchPages">
        <p id="PredictiveSearchPages" class="predictive-search__heading text-label" role="presentation">
          {{ 'search.pages' | t }}
        </p>
        {% for page in predictive_search.resources.pages %}
          <a
            id="PredictiveSearchPage-{{ forloop.index }}"
            class="predictive-search__option"
            href="{{ page.url }}"
            role="option"
            aria-selected="false"
            tabindex="-1"
          >
            {{- page.title | escape -}}
          </a>
        {% endfor %}
      </div>
    {% endif %}

    <a
      id="PredictiveSearchAll"
      class="predictive-search__option predictive-search__all"
      href="{{ routes.search_url }}?q={{ predictive_search.terms | url_encode }}&options%5Bprefix%5D=last"
      role="option"
      aria-selected="false"
      tabindex="-1"
    >
      {{- 'search.search_for_html' | t: terms: terms -}}
    </a>
  </div>
{% endif %}

{% stylesheet %}
  .predictive-search__heading {
    margin: 0;
    padding: var(--space-sm) var(--space-md) var(--space-2xs);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    opacity: var(--opacity-muted);
  }

  .predictive-search__option {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-xs) var(--space-md);
    color: inherit;
    text-decoration: none;
  }

  .predictive-search__option:hover,
  .predictive-search__option[aria-selected='true'] {
    background-color: color-mix(in srgb, currentcolor 8%, transparent);
  }

  .predictive-search__option[aria-selected='true'] {
    outline: var(--focus-ring-width) solid currentcolor;
    outline-offset: calc(var(--focus-ring-offset) * -1);
  }

  .predictive-search__image {
    flex: none;
    width: 3rem;
    height: 3rem;
    object-fit: cover;
  }

  .predictive-search__price {
    display: block;
    opacity: var(--opacity-muted);
  }

  .predictive-search__all {
    border-block-start: var(--border-width) solid var(--color-border-subtle);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.predictive_search",
  "enabled_on": {
    "groups": ["header"]
  }
}
{% endschema %}
